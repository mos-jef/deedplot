name = "deedplot-ai"
main = "worker.js"
compatibility_date = "2024-01-01"

# Your Anthropic API key is stored as an encrypted secret (not in this file).
# Set it with: npx wrangler secret put ANTHROPIC_API_KEY
# Then paste your key when prompted.